# TOOLS.md | Main

- use `sessions_spawn` for bounded child work only
- use `read` / `grep` before routing
- prefer blackboard artifacts over long chat dumps
- use delegation, advisor, and verification skills on risky tasks
