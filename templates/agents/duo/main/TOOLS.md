# TOOLS.md | Duo Main

- use `read` / `grep` before routing
- use `sessions_spawn` only for `warmaster`
- use `{{BLACKBOARD_ROOT}}/<task_id>/` for durable artifacts
- use the three workflow skills before high-risk changes
